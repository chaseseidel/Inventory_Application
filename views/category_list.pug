extends layout

block content
  h2.page-title= title
  ul.list
    each category in category_list
        li.link
            a(href=category.url) #{category.name}
    else
        li.link There are no categories.

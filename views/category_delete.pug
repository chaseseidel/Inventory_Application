extends layout

block content
  h1 #{title}: #{category.name}

  if category_products.length

    p #[strong Delete the following products before attempting to delete this category.]

    ul.list
      each product in category_products
        li.link
          a(href=product.url) #{product.name}

  else
    p Do you really want to delete this Category?

    form(method='POST' action='')
      input.form-input(type='hidden',name='categoryid', required='true', value=category._id )

      button.submit-btn(type='submit') Delete
extends layout

block content
  h2.page-title= title
  ul.list
    each product in product_list
        li.link
            a(href=product.url) #{product.name}
    else
        li.link There are no products.
